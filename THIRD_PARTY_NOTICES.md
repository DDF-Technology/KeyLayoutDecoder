# Third-party notices

Key Layout Decoder non incorpora librerie, framework o asset di terze parti.

Il programma è compilato con Microsoft Visual C++ e usa API Win32 fornite dal sistema
operativo Windows (`user32.dll` e `shell32.dll`). Microsoft e Windows sono marchi dei
rispettivi proprietari. Il progetto non è affiliato, approvato o sponsorizzato da
Microsoft.
